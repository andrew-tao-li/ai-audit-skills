# 敏感信息
.env
*.key
*.secret
*.token

# Python 缓存
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 测试产物
.test-output/
.test-output-*/
*.log.tmp

# 本地运行日志（launchd stdout/stderr 等，不入库）
*.log
# 测试运行日志（score_blackbox.py / guardrail 每次跑都会生成 *.json 临时文件，不入库）
evolution/log/

# A/B 护栏的基线快照（临时工作文件，不入库；审计轨迹在 state.json 的 guardrail_history）
evolution/guardrail/baseline.json

# LLM 生成的候选 fixture（临时 staging，review 通过后由 promote 迁入黄金集才入库）
evals/blackbox/scenarios/run-*/

# 系统文件
.DS_Store

# 编辑器
.vscode/
.idea/
