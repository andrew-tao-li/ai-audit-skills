# 开发与发布

1. 在对应 skill 的 `examples/input` 增加纯合成正例和合理异常反例。
2. 先写能验证行为的测试，再实现或修改规则。
3. 参数必须来自命令行、配置或制度文件，不把企业金额阈值写死在代码中。
4. 保持单 skill 自包含；只通过公开 JSON Schema 互操作。
5. 运行单元测试、样例端到端测试、pack 校验、密钥/网络调用扫描。
6. 复制单个 skill 到临时目录再运行一次，验证无仓库级隐藏依赖。

发布时保留版本号、脚本 hash、规则参数和已知限制。新增依赖前说明离线安装方式和无依赖降级路径。
