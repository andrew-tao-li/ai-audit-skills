# WorkBuddy 5.5.6 实机测试记录

首次测试日期：2026-09-16  
补测日期：2026-09-17  
宿主：WorkBuddy 5.5.6，macOS

## 安装过程

首次上传 `expense-audit-0.1.1.zip` 时，WorkBuddy 正确进入内置安全检测，但检测服务超时。用户随后明确允许跳过该次宿主检测，三个 `0.1.1` skill 均完成安装和发现。因此早期“安装待确认”的阻断已经解除。

## 原综合样例

三个 skill 均在 WorkBuddy 中实际读取包内合成样例并执行本地脚本：

- `expense-audit`：14 条有效记录、1 条坏行、15 条 finding、72 条 evidence。
- `procurement-fraud`：10 条 finding、71 条去重 evidence，覆盖 10 类结果，公共地址白名单有效。
- `investigation-assistant`：登记 3 个原始文件、形成 4 条时间线、隔离 2 条越界记录；15 项完整性检查通过，原件与只读副本哈希一致。

三项均生成约定产物，且没有作舞弊、串标、有罪、拒付或处分结论。原综合样例为 3/3 通过。

## 文件级回归

2026-09-17 又执行 6 个独立文件场景，结果 6/6 通过；详情见 [WorkBuddy 文件级回归测试](workbuddy-file-regression-2026-09-17.md)。测试包括正向处理和反向保护，不是常识问答。

## 隐式路由

6 条不点名 skill 的小样本中，路由正确 5/6：采购与调查正例触发，费用正例漏触发，三条反例未误触发。该结果只用于暴露明显路由问题，不能代替每个 skill 40 条的正式验收。

## 当前结论

WorkBuddy 已达到三个 `0.1.1` skill 的 Full Execution 通过标准：能够发现、执行、生成完整文件产物并遵守关键安全边界。正式 120 条隐式路由测试仍未完成。
