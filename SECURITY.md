# Security Model

## 默认控制

- 离线运行，不发起网络请求。
- 只读输入，输出写入单独目录。
- 原始文件不删除、不覆盖；正式调查先计算 SHA-256。
- 示例只使用合成数据，不包含真实员工、供应商、举报人或凭证。
- 核心脚本不读取环境变量中的 API Key，也不调用任何模型 SDK。

## 人工审批边界

下列动作不属于本包自动化范围：扩大调查对象或时间范围、联网查询、上传企业数据、冻结付款、拒绝报销、联系被调查人、处分员工、删除原始证据、形成法律或纪律认定。

## 敏感数据

在真实环境中，先确认数据授权、保留期限、访问人员、输出位置和是否允许联网。分享输出前应对身份证件、银行账号、电话、地址、邮件内容和举报人信息做最小必要脱敏。

## 报告问题

发现密钥、真实个人信息或可能破坏证据完整性的缺陷时，不要提交含原值的公开 issue；请通过仓库维护者指定的私密渠道报告，并只提供最小复现材料。
