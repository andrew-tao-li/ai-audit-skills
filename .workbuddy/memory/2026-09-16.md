# 2026-09-16

## expense-audit 显式技能调用（WorkBuddy 侧）

- 用户要求明确使用 `expense-audit` 技能，且只读 `skills/expense-audit/examples/input/expenses.csv` 与 `policy.json`，输出写到 `evals/workbuddy-output/expense-explicit`（不读取/复用其他输出目录）。
- 执行方式：Full Execution，Python `3.13.12`（managed），`run_expense_audit.py --check-env` 显示 XLSX 支持不可用但 CSV 模式正常，网络不需要。
- 结果：有效行 14、坏行 1、findings 15、evidence 72；退出码 0，manifest `network_access=false`，`skipped_rules=[]`。
- 坏行：source_row 16（EXP-BAD），原因 amount/expense_date 无法解析。
- 发现类型分布：near-duplicate 6、policy-threshold 3、weekend-signal 2、exact-duplicate-invoice 1、exact-duplicate-employee-date-amount 1、split-expense 1、robust-outlier 1；风险优先级 high 1 / medium 12 / low 2。
- 校验：15 个 finding 引用的 72 个 evidence 全部存在，无悬空引用、无孤立 evidence。
- 结论边界：输出为复核线索，非舞弊或拒付结论。

## procurement-fraud 显式技能调用（WorkBuddy 侧）

- 用户要求明确使用 `procurement-fraud` 技能，只读 `skills/procurement-fraud/examples/input/`（vendors/purchase_orders/employees/payments/bids + bid-docs/*.txt + config.json），输出写到 `evals/workbuddy-output/procurement-explicit`（新目录，不读取/复用其他输出目录）。
- 执行方式：Full Execution，Python `3.13.12`（managed），`--check-env` 通过（XLSX 不可用不影响 CSV/TXT），`skipped_modules=[]`，`network_access=false`。
- 结果：findings 10、evidence 71 条唯一行（72 个引用，EV 被两条 finding 复用）；风险优先级 high 1 / medium 7 / low 2；10 种 finding_type 各 1 条，覆盖 `examples/expected/expectations.json` 全部 must_include。
- 白名单生效：V003/V004 共享「共享企业服务中心88号」地址未被提升为 finding；`bad_rows` 0。
- 校验：悬空 evidence 引用 0，所有 finding `human_review_required=true`，`investigation_handoff.json` 的 `human_approval_required=true` 且 status=recommended；单测 5/5 OK，`evals/validate_pack.py` errors 为空。
- 结论边界：仅为复核线索，未做串标/利益输送/舞弊认定。

## investigation-assistant 显式技能调用（WorkBuddy 侧）

- 用户要求明确使用 `investigation-assistant` 技能（授权合成测试），只读 `skills/investigation-assistant/examples/input/{complaint.txt,logs.csv,messages.csv,scope.json}`，输出写到 `evals/workbuddy-output/investigation-explicit`。
- 执行方式：Full Execution，managed Python `3.13.12`（3.10+ 满足）；scope gate 通过（`authorization_confirmed=true`、`network_access=false`、allowed_sources 3 个、date_range 2026-06-01..06-30、persons E001/E002）。
- 结果与 `examples/expected/expectations.json` 一致：raw_files 3、timeline_rows 4、out_of_scope_rows 2、issues 1、findings 1；evidence 记录 11 条（3 文件 + 2 消息 + 2 日志 + 2 文本行 + 越界行 2），custody 事件 16 条（registered 3 / copied_and_verified 3 / derived 10）。
- 越界原因：`messages.csv` 第 4 行（M-OUT，人员 E999/E998 超出 persons_in_scope）、`logs.csv` 第 4 行（L-OUT，2026-07-10 超出 date_range）。
- 验证（独立只读脚本，15/15 通过）：源文件↔raw 副本↔inventory 的 SHA-256 三方一致；raw 副本 `-r--r--r--`（无写位）且实际写入抛 PermissionError；custody 的 derived 哈希与磁盘文件全匹配；30 条 evidence 引用零悬空；manifest 的 scope/script 哈希与磁盘一致且 `network_access=false`。
- 矩阵同时保留支持证据（EV-000004/6/7/9/10）与反证（EV-000005/6，即 M-001 提到的「已获主管 A100 批准」），missing_evidence 与 alternative_explanations 齐全，status=open；未做任何责任认定。
- 未联系任何人、未删除证据；输入目录 mtime 保持不变。技能单测 6/6 通过（用系统 python 跑 pytest，managed python 无 pytest）。
