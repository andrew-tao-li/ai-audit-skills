# 2026-09-17

- 运行 expense-audit 文件回归（WorkBuddy）：clean-control 与 dirty-input 合成样例各跑一次，输出至 `evals/workbuddy-output/file-regression-2026-09-17/{clean-control,dirty-input}`，逐项对照 expectations.json，两场景均 PASS（clean: 6 valid/0 bad/0 findings；dirty: 2 valid/3 bad/0 findings，坏行原因与币种默认 CNY 均符合）。回归报告：`file-regression-2026-09-17/regression-report-2026-09-17.md`。
- skill 脚本位于 `~/.workbuddy/skills/expense-audit/scripts/run_expense_audit.py`，可直接以 `--input/--policy/--output` 运行；XLSX 支持不可用但 CSV 正常。
- 运行 procurement-fraud 与 investigation-assistant 文件回归（4 场景，均 PASS）：输出至 `evals/workbuddy-output/file-regression-2026-09-17/{procurement-clean-control,procurement-dirty-input,investigation-scope-filter}`（拒绝场景按预期无目录）。报告：同目录 `regression-report-procurement-investigation-2026-09-17.md`。
- 回归要点：procurement clean 0 findings/0 bad rows/5 表全加载；dirty 3 bad rows（vendor_id 空 / unit_price 无法解析 / buyer_id 空）与 1+1 valid rows，employees·payments·bids 记 not_provided。investigation scope-filter 3 raw files、4 越界行单列、0 timeline、0 findings、issue open；authorization-denied 返回码 2 且产出前即拒绝。raw 副本 hash 与源文件一致且 `-r--r--r--`。
- 注意：`evals/workbuddy-output/file-regression-2026-09-17/` 同时存放 expense 与 procurement/investigation 结果，新增场景务必用带前缀的新子目录，避免覆盖既有基线。
- 复核结论：`evals/validate_pack.py` status=pass、errors=[]；procurement 单测 5/5、investigation 单测 6/6 通过。
